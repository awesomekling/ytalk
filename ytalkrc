# YTalk default runtime configuration

# Aliases
#alias user@ realuser@
#alias @host @realhost
#alias friend user@host

# Binding to interfaces
#localhost hostname-or-IP-address

# Default options
scrolling	= on
rering		= on
prompt_rering	= on

# Off by default (tm)
wordwrap	= off
auto_import	= off
auto_invite	= off
no_invite	= off
require_caps	= off

# Annoyances
beeps		= on
ignore_break	= off

# Command shell of preference
#shell = /bin/csh

# User interface configuration
newui		= on
history_rows	= 1000

# Color configuration
#
# Valid colors:
#   black, red, green, yellow
#   blue, magenta, cyan, white
#
# Foreground colors may be prepended with "bold-"
#
ui_colors	= red, black
menu_colors	= black, red
